import { ImageIcon } from '@/components/icons'
import { IconProps } from '@chakra-ui/react'
import React from 'react'

export const ImageBubbleIcon = (props: IconProps) => (
  <ImageIcon color="red.500" {...props} />
)
