import { WhatsappIcon as WhatsappIco } from '@/components/icons'
import { IconProps } from '@chakra-ui/react'
import React from 'react'

export const WhatsappIcon = (props: IconProps) => <WhatsappIco {...props} />
